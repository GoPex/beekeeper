# Gem sources
source "https://rubygems.org"

# Use sinatra framework for this REST api
gem 'sinatra'
# Sinatra nice additions
gem 'sinatra-contrib'

# Using puma as application server
gem 'puma'

# Docker api, we're here for that eh ?
gem 'docker-api'
